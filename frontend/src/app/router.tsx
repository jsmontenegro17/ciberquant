import {BrowserRouter,Routes,Route,Navigate} from 'react-router-dom'; import {Layout,Protected} from './App'; import {Login} from '../features/auth/Login'; import {Dashboard} from '../features/dashboard/Dashboard'; import {Accounts} from '../features/accounts/Accounts'; import {Sessions,NewSession,SessionWorkspace} from '../features/sessions/Pages'; import {Journal} from '../features/journal/Journal';
export function Router(){return <BrowserRouter><Routes><Route path="/login" element={<Login/>}/><Route element={<Protected/>}><Route element={<Layout/>}><Route path="/dashboard" element={<Dashboard/>}/><Route path="/accounts" element={<Accounts/>}/><Route path="/sessions" element={<Sessions/>}/><Route path="/sessions/new" element={<NewSession/>}/><Route path="/sessions/:id" element={<SessionWorkspace/>}/><Route path="/journal" element={<Journal/>}/></Route></Route><Route path="*" element={<Navigate to="/dashboard" replace/>}/></Routes></BrowserRouter>}
